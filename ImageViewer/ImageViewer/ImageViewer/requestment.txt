你现在是一个资深 QML 架构师兼 UI 模块化专家。  
请阅读Main.qml文件，它包含多个 UI 区块、组件、交互逻辑和动画。  
请你帮助我将这个文件进行合理拆分，便于维护与复用。  

请按以下步骤操作：

1. **代码理解与分析**  
   - 阅读整个 QML 文件。  
   - 识别其中的主要视觉区域或功能区块
   - 判断哪些部分可以抽取为独立组件

2. **模块化规划**  
   - 输出一个“模块拆分规划表”，包括：  
     - 模块名称（组件名）  
     - 职责描述  
     - 是否与其他组件交互（信号/属性/ID依赖）  
     - 建议的文件名（如 `HeaderBar.qml`）  
   - 尽量做到每个组件只负责一个 UI 区域或功能。

3. **组件拆分实现**  
   - 按照规划，把原始文件内容拆分为多个独立 QML 文件。  
   - 每个组件应包含：
     - 合理的根元素（如 `Item`, `Rectangle`, `ColumnLayout` 等）  
     - 必要的 `property`, `signal`, `id`，以便与主文件交互  
     - 避免使用全局 ID 或引用未导入的对象  
   - 主 QML 文件应保留整体布局，并通过 `import "./"` 或 `import "components"` 引用这些子组件。